<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property
            </p>
        </field>
    </record>

    <!-- Tree/List view -->
    <record id="estate_property_offer_tree_view" model="ir.ui.view">
        <field name="name">Estate Property Offer Tree View</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree editable="bottom" decoration-danger="status =='refused'" decoration-success="status == 'accepted'">
                <field name="price" />
                <field name="partner_id" />
                <field  name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accepted" string="Accepted" type="object" icon="fa-check" attrs="{'invisible':[('status','in',('accepted','refused'))]}"/>
                <button name="action_refused" string="Refused" type="object" icon="fa-times" attrs="{'invisible':[('status','in',('accepted','refused'))]}"/>
                <field name="status" invisible="1" />
            </tree>
        </field>
    </record>
    <!-- Form View -->
    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">Estate Property Offer Form View</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="form_view">
                <sheet>
                    <field name="price" />
                    <field name="partner_id" />
                    <field name="status" />
                </sheet>
            </form>
        </field>
    </record>
</odoo>
