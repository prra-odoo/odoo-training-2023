<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Blank Record(smile) -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <!--this model in \d ir_actions-->
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_available':1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
				Create your first property
            </p>
        </field>
    </record>
    <!-- Tree View -->
    <record id="estate_property_tree_view" model="ir.ui.view">
        <field name="name">Estate Properties Tree View</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree decoration-success="state in ('offer_received','offer_accepted')" decoration-muted="state == 'sold'" decoration-bf="state == 'offer_accepted'">
                <field name="name" />
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                <field name="expected_price" optional="show" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
                <field name="state" invisible="1"/>
                <field name="seq_name"/>
            </tree>
        </field>
    </record>
    <!-- Form View -->
    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">Estate Property Form View</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar" options="{'clickable':'1'}" statusbar_visible="new,offer_received,offer_accepted,sold" />
                    <button name="action_set_sold" string="Sold" type="object" class="oe_highlight"
                    states="offer_accepted" />
                    <button name="action_set_sold" string="Sold" type="object"
                    states="new,offer_received,cancelled" />
                    <button name="action_set_sold" string="Sold" type="object" state="sold"
                    invisible="1" />
                    <button name="action_set_cancel" string="Cancel" type="object"
                    attrs="{'invisible':[('state','=','sold')]}" />
                </header>
                <sheet>
                    <widget name="web_ribbon" title="sold" bg_color="bg-danger" attrs="{'invisible': [('state', 'in', ('offer_recived','new','canceled','offer_accepted'))]}"/>
                    <widget name="web_ribbon" title="Accepted" bg_color="bg-success" attrs="{'invisible': [('state', 'in', ('offer_recived','new','canceled','sold'))]}"/>
                    <div>
                        <label for="name" string="Property Name" />
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group name="main">
                        <group name="group_left">
                            <field name="property_type_id" options="{'no_create': True , 'no_edit': True}"/>
                            <field name="postcode" />
                            <field name="date_availability" string="Available From"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                        </group>
                        <group name="group_right">
                            <field name="expected_price" />
                            <field name="best_price"/>
                            <field name="selling_price" />
                            <field name="seq_name"/>
                        </group>
                    </group>
                    <!-- Add a menu tab below in the form -->
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" attrs="{'invisible':[('garden','=',False)]}" />
                                <field name="garden_orientation" attrs="{'invisible':[('garden','=',False)]}"  />
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="offers">
                            <field name="offer_ids"  nolabel="1" attrs="{'readonly':[('state','in',('offer_accepted','sold','canceled'))] }" />
                        </page>
                        <page string="other info">
                            <group>
                                <field name="user_id" />
                                <field name="buyer" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="message_ids" />
                    <field name="activity_ids" />
                </div>
            </form>
        </field>
    </record>
    <!-- Search View -->
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">Estate Property Search View</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area"  filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades" />
                <filter string="Available" name="available" domain="[('state','in', ('new','offer_received'))]"/>
                <group string="Group By">
                    <filter string="Postcode" name="gorupby_postcode" context="{'group_by':'postcode'}" />
                    <filter string="Garden" name="gorupby_garden" context="{'group_by':'garden'}" />
                </group>
            </search>
        </field>
    </record>


        <!-- ir sequence  -->
    <record id="seq_task_order" model="ir.sequence">
        <field name="name">Property Reference</field>
        <field name="code">estate.property</field>
        <field name="prefix">TA</field>
        <field name="padding">3</field>
        <field name="company_id" eval="False"/>
    </record>


    <!-- kanban view-->
    <record id="estate_property_kanban_view" model="ir.ui.view">
        <field name="name">Estate Property kanban View</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable = "False" >
                <field name="state"/>
                <field name="color"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="{{'oe_kanban_color_' + kanban_getcolor(record.color.raw_value) }} oe_kanban_global_click">                 
                            <div class="oe_kanban_global_click">
                                <div>
                                    <field name="favorite" widget ="boolean_favorite" nolabel="1" class="me-2"/>    
                                    <strong><field name="name" widget="badge"/></strong> 
                                </div>
                                <div t-if="record.buyer.raw_value">
                                    <i class="fa fa-user me-2" title="buyer"/>
                                    <strong><field name="buyer" widget="many2one_avatar_partner"/></strong>
                                </div>
                                <div>
                                    Expected Price:<field name="expected_price"/>
                                </div>
                                <div t-if="record.state.raw_value == 'offer_received'">
                                    Best Price: <field name="best_price"/>
                                </div>
                                <div t-if="record.state.raw_value == 'offer_accepted'">
                                    Selling Price: <field name="selling_price"/>
                                </div>
                                <div>
                                    <field name="tag_ids" widget="many2many_tags" options ="{'color_field':'color'}"/>
                                </div> 
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="kanban_state" widget="state_selection"/>
                                        <field name="user_id" widget="many2one_avatar_user"/>
                                    </div>
                                </div>   
                            </div>
                        </div>    
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>