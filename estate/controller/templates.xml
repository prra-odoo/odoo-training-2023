<odoo>
    <data>
       <record id="test_menu" model="website.menu">
           <field name="name">Properties</field> <!-- Name of the menu-->
           <field name="url">/estate</field>
           <field name="parent_id" ref="website.main_menu"/>
           <field name="sequence" type="int">85</field>
              <!--sequence of the menu-->
       </record>
       <!-- <record id="demo_page" model="website.page">
         <field name="name">Property</field>
         <field name="website_published">True</field>
         <field name="url">/properties</field>
         <field name="type">qweb</field>
         <field name="key">web_page.property_page</field>
         <field name="arch" type="xml">
             <t t-name="web_page.property_page">
                             
             </t>
         </field>
     </record> -->
   </data>
    <!-- <template id="index">
        <t t-call="website.layout">
            <t t-set="title">Property</t>
            <div class="oe_structure">
                <div class="container">
                    <t t-foreach="teachers" t-as="teacher">
                        <p><t t-esc="teacher.id"/> <t t-esc="teacher.name"/></p>
                    </t>
                </div>
            </div>
        </t>
    </template> -->
    <record id="property_page" model="website.page">
        <field name="name">Demo</field>
        <field name="website_published">True</field>
        <field name="url">/demo</field>
        <field name="view_id" ref="demo_page_template"/>
        <!-- Provide  template id that you want to render --> 
    </record>
    <template id="demo_page_template">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container">
                   <!-- Page Contents -->                      
                </div>
            </div>
        </t>
    </template>
</odoo>