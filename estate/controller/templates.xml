<?xml version="1.0" encoding="utf-8"?>
    <odoo>
        <template id="properties_template">
            <html>
                <head>
                    <title t-esc="status_message">Properties</title>
                    <link rel="stylesheet" href="/web/static/lib/bootstrap/dist/css/bootstrap.css"/>
                    <style>
                        .card:hover {
                            transform: scale(1.05);
                            box-shadow: 0 10px 20px rgba(0,0,0,.12), 0 4px 8px rgba(0,0,0,.06);  
                            transition: all 0.2s ease-in;
                                }
                    </style>
                </head>
                <body>
                    <t t-call="website.layout">
                        <form action="/properties/">
                            <t t-call="website.website_search_box">
                                <t t-set="action" t-value= "'/properties/'"/>
                            </t>
                        </form>
                        <t t-set="title">Properties</t>
                        <div class="oe_structure">
                            <div class="container">
                                    <!-- <p><t t-esc="property.id"/> <t t-esc="property.name"/></p> -->
                                    <div class="container text-center">
                                        <div class="row row-cols-4">
                                            <t t-foreach="properties" t-as="property">
                                                <div class="col">
                                                    <div class="card shadow-lg p-3 mb-5 mt-4 " style="width: 18rem;">
                                                        <t t-if="property.image">
                                                            <img t-attf-href="/properties/{{ slug(property) }}" t-att-src="image_data_uri(property.image)" width="300" height="250" alt="image of properties" class="oe_avatar"/>
                                                        </t>
                                                            <div class="card-body">
                                                            <h5 class="card-title"><span t-field="property.name"/></h5>
                                                            <span t-esc="property.date_availability" />
                                                            <div>Expected Price:<span t-esc="property.expected_price"/></div>
                                                            <center><a t-attf-href="/properties/{{ slug(property) }}" class="btn btn-info mt-4" width="100%">View more</a></center>
                                                        </div>
                                                    </div>
                                                </div>
                                            </t>
                                        </div>
                                    </div>
                            </div>
                        </div>
                        <div class="d-flex justify-content-center my-3">
                            <t t-call="website.pager"/>
                        </div>
                    </t>
                </body>
            </html>
        </template>
        
       <template id="template_controller">
    <html>
        <head>
            <title t-esc="status_message">Properties</title>
            <link rel="stylesheet" href="/web/static/lib/bootstrap/dist/css/bootstrap.css"/>
            <style>
                .prop_img{
                    height:30vw;
                    width:30vw;
                }
            </style>
        </head>
        <body>
            <t t-call="website.layout">
                <div class="oe_structure">
                    <div class="container">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="http://localhost:8069/properties">Properties</a></li>
                                <li class="breadcrumb-item active" aria-current="page"><span t-field="prop.name"/></li>
                            </ol>
                        </nav>
                        <div style="width:50vw;" class="card shadow-lg p-4 mb-5 mt-4">
                            <t t-if="prop.image">
                                <img t-att-src="image_data_uri(prop.image)" alt="image" class="prop_img"/>
                            </t>
                            <div class="card-body text-center">
                                <h5 class="card-title"><span t-field="prop.name"/></h5>
                                <span t-esc="prop.date_availability" />
                                <div>Expected Price: <span t-field="prop.expected_price"/></div>
                                <div>Selling Price: <span t-field="prop.selling_price"/></div>
                                <div>Salesman: <span t-field="prop.seller_id"/></div>
                            </div>
                        </div>
                        <div style="width:50vw;" class="card shadow-lg p-4 mb-5 mt-4">
                            <h5 class="card-title">Offers</h5>
                            <table class="table">
                            <thead>
                                <tr>
                                    <th>Price</th>
                                    <th>Partner</th>
                                    <th>Validity(days)</th>
                                    <th>Deadline</th>
                                    <th>State</th>
                                </tr>
                            </thead>
                    <tbody>
                        <t t-set="offers" t-value="prop.mapped('offer_ids')"/>
                        <tr t-foreach="offers" t-as="offer">
                            <td>
                                <span t-field="offer.price"/>
                            </td>
                            <td>
                                <span t-field="offer.partner_id"/>
                            </td>
                            <td>
                                <span t-field="offer.validity"/>
                            </td>
                            <td>
                                <span t-field="offer.date_deadline"/>
                            </td>
                            <td>
                                <span t-field="offer.status"/>
                            </td>
                        </tr>
                    </tbody>
                </table>
                        </div>
                    </div>
                </div>
                <div class="oe_structure"/>
            </t>
        </body>
    </html>
</template>

</odoo>



