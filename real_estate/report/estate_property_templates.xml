<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="report_property_offers">
        <t t-foreach="docs" t-as="property">
            <t t-call="web.html_container">
                <t t-call="web.external_layout">
                    <div class="page">
                        <h2>
                            <span t-field="property.name"/>
                        </h2>
                        <div>
                            <strong>salesman: </strong>
                            <span t-field="property.salesperson_id"/>
                        </div>
                        <t t-if="len(property.mapped('offer_ids')) > 0" t-call="real_estate.table_template_offers_in_sub"/>
                        <p t-else="">No offers!</p>
                    </div>
                </t>
            </t>
        </t>
    </template>

     <!-- table template -->

    <template id="table_template_offers_in_sub">
        <div>
            <strong>Expected Price: </strong>
            <span t-field="property.expected_price"/>
        </div>
        <div>
            <strong>Status: </strong>
            <span t-field="property.state"/>
        </div><br/>
        <t t-if="property.offer_ids">
            <table class="table">
                <thead>
                    <tr>
                        <th>Price</th>
                        <th>Partner</th>
                        <th>Validity (days)</th>
                        <th>Deadline</th>
                        <th>State</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-set="offers" t-value="property.mapped('offer_ids')"/>
                        <tr t-foreach="offers" t-as="offer">
                            <td>
                                <span t-field="offer.price"/>
                            </td>
                            <td>
                                <span t-field="offer.partner_id"/>
                            </td>
                            <td>
                                <span t-field="offer.validity"/>
                            </td>
                            <td>
                                <span t-field="offer.date_deadline"/>
                            </td>
                            <td>
                                <span t-field="offer.status"/>
                            </td>
                        </tr>
                </tbody>
            </table>     
        </t>
        <t t-else="">
            <div>No offers for the given property</div>
        </t> 
    </template>

    <template id="report_res_users">
        <t t-foreach="docs" t-as="salesperson">
            <t t-call="web.html_container">
                <t t-call="web.external_layout">
                    <div class="page">
                        <h2>
                            <strong>Salesman:</strong>
                            <span t-field="salesperson.name"/>
                        </h2>
                        <t t-if="salesperson.property_ids">      
                            <t t-set="properties" t-value="salesperson.mapped('property_ids')" />
                            <t t-foreach="properties" t-as="property">
                                <h2>
                                    <span t-field="property.name" />
                                </h2>
                                <t t-call="real_estate.table_template_offers_in_sub"/>
                            </t>
                        </t>
                        <p t-else="">This user has no properties</p>
                    </div>
                </t>
            </t>
        </t>
    </template>

</odoo>